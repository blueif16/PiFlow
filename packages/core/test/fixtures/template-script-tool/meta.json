{
  "id": "template-script-tool",
  "name": "template-script-tool",
  "description": "Fixture: a node declares tool:demo_probe with an EXPLICIT defs override (a shared tools dir outside the template, via {{WORKSPACE}}) + a real tiny script — the DECLARE→DEFINE→DISCOVER→INJECT→PREFLIGHT happy path.",
  "phases": ["run"]
}
