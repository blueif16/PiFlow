{
  "id": "probe",
  "phase": "run",
  "deps": [],
  "prompt": { "file": "prompt.md" },
  "tools": {
    "allow": ["read", "tool:demo_probe"],
    "defs": { "tool:demo_probe": "{{WORKSPACE}}/shared-tools/demo_probe_broken" }
  },
  "contract": {
    "artifacts": ["out.json"],
    "owns": ["out.json"],
    "readScope": ["{{RUN}}"]
  }
}
