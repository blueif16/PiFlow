---
"@piflow/cli": minor
---

`piflowctl understand` grows the reconcile suite: `--reconcile` (post-merge advisory pass — E4 body-hash, E5 impact, coverage rungs) and `--owns <path>` (reverse code→card lookup) route to the one OKF engine; the FIND ranker gains a tokenized phrase fallback gated structurally by the ownership law (a natural-language question now finds its owning card — no stopword list); and the optimizer's fixer is wired to FIND — a pointer-less defect whose structured signals rank a card at ownership strength gets that slice inlined into its scope-context with explicit weaker-provenance marking (`enrichCodeMap` fallback + `findSliceForDefect`).
